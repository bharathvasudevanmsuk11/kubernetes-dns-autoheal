apiVersion: v1
kind: Service
metadata:
  name: dns-throttle-monitor
  namespace: monitoring
  labels:
    app: dns-throttle-monitor
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: dns-throttle-monitor
  ports:
  - name: metrics
    port: 9100
    targetPort: 9100
    protocol: TCP
