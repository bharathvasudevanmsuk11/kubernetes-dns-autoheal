apiVersion: v1
kind: Service
metadata:
  name: remediation-webhook
  namespace: monitoring
  labels:
    app: remediation-webhook
spec:
  type: ClusterIP
  selector:
    app: remediation-webhook
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
